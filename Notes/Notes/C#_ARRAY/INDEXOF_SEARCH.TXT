//-----------------------ARRAY INDEXOF YÖNTEMÝYLE ARAMA YAPMAK----------------------

string[] AracListe = new string[] { "Opel", "Peugeot", "Mercedes", "Ferrari", "Reno", "Fiat", "Golf" };
int index = Array.IndexOf(AracListe, txt_Ara.Text);
if (index >= 0)
  MessageBox.Show("Araç " + index.ToString() + ". sýrada bulundu.");
else
   MessageBox.Show("Aradýðýnýz araç listede mevcut deðil.");
