public int[] bubbleSort(int [] array){
    int n = array.length;
    for (int i=1; i < n; i++) 
    {
      //Elemanlar ikiser ikiser karþýlaþtýrýlýp küçük olan sol tarafa alýnýyor.
        for (int j=0; j < n-1; j++) {
            if (array[j] > array[j+1]) {
                
              //elemanlarýn yeri deðiþtiriliyor.
                int temp = array[j];
                array[j] = array[j+1];
                array[j+1] = temp;
            }
        }
    }
    return null;
}
